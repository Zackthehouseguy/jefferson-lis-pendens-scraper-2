name: Render Stress50 Results
on:
  push:
    branches: [main]
    paths:
      - 'scrapers/probe/render_stress50_results.py'
      - 'scrapers/probe/split_stress50_ai_input.py'
      - '.github/workflows/render-stress50-results.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m scrapers.probe.render_stress50_results
      - run: python -m scrapers.probe.prepare_stress50_ai_input
      - run: python -m scrapers.probe.split_stress50_ai_input
      - name: Persist result sheets and AI input chunks
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/stress_50_live/results_*.md reports/stress_50_live/ai_input*.json
          git diff --cached --quiet || git commit -m "Render stress50 review chunks [skip ci]"
          git push origin HEAD:main
